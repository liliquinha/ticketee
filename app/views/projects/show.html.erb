<% title(@project.name, "Projects") %>

<%= @project.name %> <%= link_to "Editar Projeto", edit_project_path(@project) %> |
<%= link_to "Delete", @project, method: :delete, confirm: "Are you sure?" %>